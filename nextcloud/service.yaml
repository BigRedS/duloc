apiVersion: v1
kind: Service
metadata:
  name: nextcloud
  namespace: nextcloud
  annotations:
    tailscale.com/expose: "true"
    tailscale.com/hostname: "nextcloud"  # your desired Tailscale hostname
spec:
  type: LoadBalancer
  selector:
    app: nextcloud
  ports:
    - port: 80
